# wtn command line

`wtn` is installed with the package. Every command reads `WITAN_API_KEY` and `WITAN_BASE_URL` from the
environment (see [Configuration](../guide/configuration.md)); `--json` prints the API's answer as JSON
where a command supports it. The help below is generated from this version's parser.

<!-- wtn-reference -->
